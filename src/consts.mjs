const FORMAT = {
  JSON: 'json',
  CSV: 'csv',
  HTML: 'html'
};

const METHOD = {
  BLAME: 'blame',
  DIFF: 'diff'
};

const PERIOD = {
  MONTH: 'month',
  YEAR: 'year'
};

export {
  FORMAT,
  METHOD,
  PERIOD
};
