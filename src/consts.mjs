const FORMAT = {
  JSON: 'json',
  CSV: 'csv',
  HTML: 'html'
};

const DETAIL = {
  BLAME: 'blame',
  DIFF: 'diff'
};

const PERIOD = {
  MONTH: 'month',
  YEAR: 'year'
};

export {
  FORMAT,
  DETAIL,
  PERIOD
};
